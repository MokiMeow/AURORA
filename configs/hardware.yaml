training:
  preferred_gpu_memory_gb: 24
  fallback:
    mode: cpu
    precision: 8bit
    gradient_checkpointing: true

inference:
  planner:
    max_batch_size: 1
    timeout_seconds: 120

firecracker:
  kernel_image: artifacts/kernel.bin
  rootfs_image: artifacts/rootfs.ext4

