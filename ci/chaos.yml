name: Chaos Tests

on:
  workflow_dispatch:

jobs:
  chaos:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run chaos script
        run: bash scripts/chaos_tests.sh


