name: Evaluate SWE-Bench Live (Manual)

on:
  workflow_dispatch:

jobs:
  swe-bench-live:
    uses: ./.github/workflows/eval-suite.yml
    with:
      suite: swe-bench-live
      python-version: "3.11"


