name: Evaluate SWE-Bench Lite (Manual)

on:
  workflow_dispatch:

jobs:
  swe-bench-lite:
    uses: ./.github/workflows/eval-suite.yml
    with:
      suite: swe-bench-lite
      python-version: "3.11"

