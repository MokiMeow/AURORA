# NOTE: Mirror of ci/eval-nightly.yml for GitHub Actions execution.
name: Evaluate (Nightly)

on:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:

jobs:
  swe-bench-lite:
    uses: ./.github/workflows/templates/eval-suite.yml
    with:
      suite: swe-bench-lite
      python-version: "3.11"

