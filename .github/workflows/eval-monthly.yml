name: Evaluate SWE-Bench Full (Manual)

on:
  workflow_dispatch:

jobs:
  swe-bench-full:
    uses: ./.github/workflows/eval-suite.yml
    with:
      suite: swe-bench-full
      python-version: "3.11"


